// Copy this file to secrets.h and fill in your real values.
// secrets.h is gitignored, so it will never be committed.

#pragma once

#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
